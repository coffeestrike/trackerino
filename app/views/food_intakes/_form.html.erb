<%= form_with(model: food_intake) do |form| %>
  <% if food_intake.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(food_intake.errors.count, "error") %> prohibited this food_intake from being saved:</h2>

      <ul>
        <% food_intake.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :intake_time, style: "display: block" %>
    <%= form.datetime_field :intake_time, value: food_intake.intake_time || Time.zone.now, step: 'any' %>
  </div>

  <div>
    <%= form.label :calories, style: "display: block" %>
    <%= form.text_field :calories %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
