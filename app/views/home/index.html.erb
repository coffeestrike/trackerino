<div class="mb-4">
  <h2> 🌮 Food </h2>
  <div id="food_intakes">
    <table>
      <tr>
        <th class="name">Name</th>
        <th>Amount</th>
        <th>Intake time</th>
        <th colspan="2">Actions</th>
      </tr>
      <% @food_intakes.each do |food_intake| %>
        <%= render food_intake %>
      <% end %>
    </table>
  </div>
  <hr/>
  <%= button_to "New food intake", new_food_intake_path, method: :get, class: "btn btn-primary" %>
</div>

<div class="mb-4">
  <h2>☕ Fluids</h2>
  <div id="fluid_intakes">
    <table>
      <tr>
        <th class="name">Name</th>
        <th>Amount</th>
        <th>Intake time</th>
        <th colspan="2">Actions</th>
      </tr>
      <% @fluid_intakes.each do |fluid_intake| %>
        <%= render fluid_intake %>
      <% end %>
    </table>
  </div>
  <hr/>
  <%= button_to "New fluid intake", new_fluid_intake_path, method: :get, class: "btn btn-primary" %>
</div>